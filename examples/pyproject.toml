[project]
name = "ml-experiments"
version = "0.1.0"
description = "Example ML experiments demonstrating auto-detected prefix"
requires-python = ">=3.9"
dependencies = [
    "ml-dash",
]

[tool.uv.sources]
ml-dash = { path = "..", editable = true }
